- name: Install MySQL Client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: Load Schema
  ansible.builtin.shell: mysql -h mysql-dev.pdevops562.online -u root -p RoboShop@1 < /app/schema/{{component}}.sql